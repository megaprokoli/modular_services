from setuptools import setup

setup(
    name='modular_services',
    version='0.0.1',
    packages=['modular_services'],
    install_requires=[],
    url='https://github.com/megaprokoli/modular_services.git',
    license='',
    author='megaprokoli',
    author_email='',
    description='A small lib to dynamically load services (plugin) into your application.'
)